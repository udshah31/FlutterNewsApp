import 'package:flutter/material.dart';

class PrawasPage extends StatelessWidget {
  const PrawasPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}